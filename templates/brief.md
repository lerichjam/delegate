# Brief NNN: <title>

## Goal
<The user-visible outcome, one or two sentences.>

## Context
<Files already located, each with why it matters. Seeds the executor's
scan; does not replace it.>
- `path/to/file.ext` — <why>

## Architecture
<Components, responsibilities, and the boundaries between them.>

## Contracts
<Exact signatures, types, schemas, error shapes. Be specific enough that
two engineers would write the same interface. This is the section that
determines whether delegation works.>

## Implementation notes
<Algorithm, edge cases, error handling. Include explicit "do not do X"
warnings where a plausible-but-wrong approach exists.>

## Style constraints
<Name the specific existing file(s) whose patterns the new code must
match.>

## Verification
<Exact commands, with expected outcomes.>
- `<command>` — expect <outcome>

## Acceptance criteria
<Concrete and checkable. Not "works well" — state what is observably true
when this is done.>
- [ ] <criterion>

## Out of scope
<Explicit. Prevents scope creep by a model that likes being helpful.>
